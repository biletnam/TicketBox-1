@model TicketBox.WebUI.Models.Event

@*<div class="well">
    <h3>
        <strong>@Model.Name</strong>
    </h3>
    <span class="lead">@Model.Description</span>
    <img class="img-thumbnail img-responsive" src="~/Content/images/@Model.PathToImage" />
    <p>@Model.Location</p>
    @using (Html.BeginForm("AddToCart", "Cart"))
    {
        <div class="pull-right">
            @Html.HiddenFor(x => x.EventId)
            @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
            <input type="submit" class="btn btn-success" value="Добавить в корзину" />
        </div>
    }
    <p>@Model.TimeEvent</p>    
</div>*@


<div class="grid1_of_3">    
    <a href="/Home/Details/@Model.EventId">
        <img src="~/Content/images/@Model.PathToImage" alt="" />
        <h3>@Model.Name</h3>
        <div class="price">
            <h4><span>Детали</span></h4>
        </div>
        <span class="b_btm"></span>
    </a>   
</div>
            